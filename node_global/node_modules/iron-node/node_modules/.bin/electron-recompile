#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_electron-recompile@1.0.16@electron-recompile/lib/index.js" "$@"
  ret=$?
else 
  node  "$basedir/../_electron-recompile@1.0.16@electron-recompile/lib/index.js" "$@"
  ret=$?
fi
exit $ret
